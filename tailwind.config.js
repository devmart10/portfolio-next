const { colors } = require("tailwindcss/defaultTheme");

module.exports = {
  theme: {
    extend: {
      colors: {
        primary: colors.green["800"],
      },
    },
  },
  variants: {},
  plugins: [],
};
