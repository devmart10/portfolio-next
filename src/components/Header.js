import React, { useState } from "react";

const Header = () => {
  return (
    <header className="mb-4 shadow-lg bg-primary">
      <div className="flex items-center p-2 my-container">
        <h3 className="mb-0 font-semibold tracking-wider text-gray-100">Devon Martin</h3>
      </div>
    </header>
  );
};

export default Header;
